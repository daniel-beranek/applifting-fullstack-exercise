name: renovate-bot

on:
  schedule:
    - cron: '*/15 * * * *'

jobs:
  renovate:
    runs-on: ubuntu-latest

    steps:
      - name: Renovate Bot GitHub Action
        uses: renovatebot/github-action@v41.0.6
